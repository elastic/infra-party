### Example environment for fixture scripts (fish shell format)
# Copy to config.env and adjust values as needed.
#
# These variables feed both the Terraform configuration in ./terraform and
# the wrapper scripts in this directory. The scripts will generate
# terraform/terraform.tfvars automatically if it does not already exist.

set -gx PROJECT_ID "my-gcp-project"
set -gx REGION "us-central1"
set -gx ZONE "us-central1-b"
# Optional: change the prefix to keep resources grouped per run/team.
set -gx RESOURCE_PREFIX "gcp-fixture"

# Select the fixture scenario to deploy: "vpc-flow", "nlb", or "alb".
set -gx SCENARIO "vpc-flow"

# For alb scenario: set the load balancer scope to "global" or "regional" (default: regional).
set -gx LOAD_BALANCER_SCOPE "regional"
